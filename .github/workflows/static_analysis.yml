name: Static Analysis
on: [pull_request, workflow_dispatch]
jobs:
  diffuse:
    name: Diffuse AAR Analysis
    runs-on: macOS-11
    steps:
      # Set up environment to assemble SDK
      - name: Set up Java 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'zulu'

      # Assemble artifacts from main branch
      - name: Checkout Main Branch
        uses: actions/checkout@v2
        with:
          ref: master
      - name: Assemble Release AAR on Main Branch
        run: ./gradlew clean assembleRelease
      - name: Upload Main Branch Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: main-aar
          path: |
            Drop-In/build/outputs/aar/Drop-In-release.aar
            Demo/build/outputs/aar/Demo-release.aar
      # Assemble artifacts from current branch
      - name: Checkout Current Branch
        uses: actions/checkout@v2
      - name: Assemble Release AAR on Current Branch
        run: ./gradlew clean assembleRelease
      - name: Upload Current Branch Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: current-aar
          path: |
            Drop-In/build/outputs/aar/Drop-In-release.aar
            Demo/build/outputs/aar/Demo-release.aar
      # Set up Diffuse
      - name: Install Diffuse
        run: brew install JakeWharton/repo/diffuse
      - name: Download Workflow Artifacts 
        uses: actions/download-artifact@v2
      - name: Set up Java 11
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '11'

      # Run Diffuse analysis
      - name: Run Diffuse Analysis - BraintreeDropIn
        run: diffuse diff --aar main-aar/Drop-In/build/outputs/aar/Drop-In-release.aar current-aar/Drop-In/build/outputs/aar/Drop-In-release.aar
      - name: Run Diffuse Analysis - Demo
        run: diffuse diff --aar main-aar/Demo/build/outputs/aar/Demo-release.aar current-aar/Demo/build/outputs/aar/Demo-release.aar
